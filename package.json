{
  "name": "donkey-website",
  "version": "1.0.0",
  "description": "Educational website about donkey breeds, care, and facts",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "npm run test:unit && npm run test:integration",
    "test:unit": "node --experimental-vm-modules node_modules/.bin/jest tests/build tests/scripts",
    "test:integration": "node --experimental-vm-modules node_modules/.bin/jest tests/integration --runInBand",
    "test:e2e": "npm run dev & wait-on http://localhost:8080 && jest tests/e2e && kill %1",
    "test:lighthouse": "npm run dev & wait-on http://localhost:8080 && jest tests/e2e/lighthouse.test.js && kill %1",
    "test:accessibility": "npm run dev & wait-on http://localhost:8080 && jest tests/e2e/accessibility.test.js && kill %1",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "build": "node build/build.js",
    "dev": "node build/dev-server.js",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "donkey",
    "education",
    "static-site",
    "jamstack"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@axe-core/puppeteer": "^4.8.2",
    "@jest/globals": "^29.7.0",
    "chrome-launcher": "^1.1.0",
    "clean-css": "^5.3.2",
    "gray-matter": "^4.0.3",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "jest-image-snapshot": "^6.4.0",
    "jsdom": "^23.0.1",
    "lighthouse": "^11.4.0",
    "marked": "^9.1.6",
    "puppeteer": "^21.6.1",
    "sharp": "^0.32.6",
    "wait-on": "^7.2.0"
  }
}
